
jQuery(document).ready(function($) {
  /*
  var title = $("header h1.entry-title");
  title.text(shortcode_vars.year + " " + title.text());
  var title = $("main h1.wp-block-post-title");
  title.text(shortcode_vars.year + " " + title.text());
  */
});

